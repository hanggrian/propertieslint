name: Setup Workspace
description: Checkout and install all dependencies

runs:
  using: composite
  steps:
  - uses: actions/setup-go@v6
    with:
      go-version: "1.26"

  - shell: bash
    run: go mod download
